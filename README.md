# 🎓 STUDENTO - Student Management System

A lightweight, console-based Java application designed to manage student records efficiently using Object-Oriented Programming (OOP) principles.

## 🚀 Features
* **Add New Students**: Capture student names, IDs, and grades.
* **View Records**: Display a formatted list of all registered students.
* **Search Functionality**: Quickly find students by their unique ID.
* **Delete Records**: Remove students from the system dynamically.

## 🛠️ Tech Stack
* **Language:** Java
* **Data Structures:** ArrayList (for dynamic storage)
* **Tools:** VS Code, Git

## 📂 Project Structure
- `Main.java`: The entry point containing the user menu loop.
- `Student.java`: The blueprint (Model) for student objects.
- `StudentManager.java`: Handles the logic for managing the student list.

## ⚙️ How to Run
1. Clone the repo: `git clone https://github.com/ayoubchorfane/STUDENTO.git`
2. Compile the files: `javac src/*.java`
3. Run the app: `java -cp src Main`
